#include"main.h"
/**
 * add - adds two integers and returns the result
 * @a: int to be added
 * @b: int to be added
 * Return: Results
 */
int add(int a, int b)
{
	int i;

	i = (a + b);

	return (i);
}
